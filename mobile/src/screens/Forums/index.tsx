/* eslint-disable import/no-unresolved */
import { Header } from '@src/components/Header/index';
import { Box, Heading, ScrollView, Text } from 'native-base';

export const Forums = () => {
  return (
    <Header
      title={'Olá, Katarina'}
      description={'Encontre as categorias de seu interesse'}
    ></Header>
  );
};
